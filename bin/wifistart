#!/bin/sh
killall wpa_supplicant
killall dhcpcd
killall dhclient

ip a f dev wlan0

ip link set wlan0 up

wpa_supplicant -Dnl80211 -iwlan0 -c/root/wifi.conf&

sleep 5
dhcpcd --noarp wlan0
ping -c 4 google.com
