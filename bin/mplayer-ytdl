#!/bin/bash

echo "$0 $@" > /tmp/test.log

dir="$(mktemp -d)"

format='best[height<=720]/best'

mplayer -cookies -cookies-file "$dir/cookie" "$(youtube-dl -f "$format" -g --cookies "$dir/cookie" "$1")"

rm -r "$dir"

