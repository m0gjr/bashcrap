#!/bin/bash

[ -z "$1" ] && exit 1

cd /home/blog



file=_posts/$(date +%F)-$1.md

nano $file

read -p "push [y]/n? " -n1 push
echo

if ! [ "$push" == "n" ]
then
	git add *
	git commit -am "$1"
	git push
fi
