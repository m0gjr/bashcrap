#!/bin/bash

if [ "$(basename "$0")" == "x-terminal-emulator-float" ]
then
	if [ -n "$DISPLAY" ]
	then
		exec xterm -class UXTerm -e "$@"
	else
		exec foot --app-id="term-float" "$@"
	fi
else
	if [ -n "$DISPLAY" ]
	then
		exec xterm -e "$@"
	else
		exec foot "$@"
	fi
fi
