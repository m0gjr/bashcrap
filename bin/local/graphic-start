#!/bin/bash

export GTK_THEME=Adwaita:dark

if [ -f /etc/local/graphic-start ]
then
	exec /etc/local/graphic-start
elif [ -f /usr/bin/startx ]
then
	export i3_display=x11
	exec startx
else
	export i3_display=wayland
	exec sway -c /etc/xdg/i3/config
fi
